# AOC Analytics Environment Configuration
# Copy from JFK backend/.env or set your own

# LLM Provider (openai or ollama)
LLM_PROVIDER=openai

# OpenAI Configuration
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=

# Database Configuration
# Production uses PostgreSQL (set DATABASE_URL)
# Development uses SQLite (leave DATABASE_URL empty)
# DATABASE_URL=postgresql://user:pass@host:5432/dbname
AOC_DB_PATH=aoc_sales.db

# Optional: Ollama for local LLM (if no GPU, use OpenAI instead)
# OLLAMA_MODEL=llama3.2:3b
